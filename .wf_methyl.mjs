export const meta = {
  name: 'biofs-methyl-verb-build',
  description: 'Design, draft, and adversarially review the biofs methyl command GROUP (submit + exec subcommands) and the additive biofs-node inputType=methyl branch, protocol-native (no orphan shell scripts, no flat hyphenated verbs).',
  phases: [
    { title: 'Research' },
    { title: 'Draft' },
    { title: 'Review' },
    { title: 'Finalize' },
  ],
}

const GROUND = [
'PROJECT: GenoBank biofs. Build an Oxford-Nanopore 5mCG/5hmCG methylation pipeline that is 100% protocol-native, as a SINGLE biofs command GROUP named methyl with two subcommands (NOT two flat top-level verbs, and NOT a hyphenated methyl-exec verb — Daniel explicitly rejected verb sprawl):',
'  - biofs methyl <serial> --bams <csv>   = the CLIENT (submit). It is the DEFAULT subcommand, so both "biofs methyl <serial>" and "biofs methyl submit <serial>" work.',
'  - biofs methyl exec <flags>            = the EXECUTOR subcommand. biofs-node invokes it via spawn of the biofs binary with arg list [methyl, exec, ...flags]. It runs the pipeline on the GPU/CPU VM.',
'HARD DIRECTIVE from Daniel: NO orphan shell script on the VM (no clara-methyl-job.sh). The runner logic lives in the biofs methyl exec subcommand, npm-versioned and auditable. Mirror the existing group+subcommand pattern used by biofs job recall / biofs annotate submit (a parent command.command(...) group with .command(sub) children); the directory layout is src/commands/methyl/ with submit.ts and exec.ts, registered as a group in src/index.ts.',
'',
'KEY FILES (all readable locally):',
'- biofs-cli source: /Users/danieluribe/Downloads/biofs-cli (src/commands, src/index.ts). Read existing GROUP verbs to match conventions EXACTLY: src/commands/job/ (recall.ts, status.ts) and how the job group is registered in src/index.ts (const jobCmd = program.command(job); jobCmd.command(recall <id>)...). Also src/commands/annotate/submit.ts, src/commands/fingerprint.ts.',
'- biofs-cli installed GLOBAL (the one that actually runs): /opt/homebrew/lib/node_modules/@genobank/biofs (v3.11.0, dist). npm publish token is BROKEN, so new verbs are hand-ported: compile to dist/commands/methyl/submit.js + dist/commands/methyl/exec.js and merge the methyl group registration into the global dist/index.js. Mirror the .ts into the local clone for provenance.',
'- biofs-node source: /Users/danieluribe/Downloads/biofs-node/src/index.js (2009 lines, @genobank/biofs-node v0.4.4). DEPLOYED copy: /opt/biofs-node-v0.4/src/index.js (1899 lines) on GCE VM genobank-production (reach: gcloud compute ssh genobank-production --tunnel-through-iap --zone=us-central1-a). Local is 110 lines AHEAD of deployed; express the patch so it applies to BOTH, with a reconciliation note. biofs-node listens on port 8787; nginx aliases https://genobank.app/api_biofs_node/* to localhost:8787/agent/*.',
'',
'biofs-node INTEGRATION FACTS (from local src/index.js, verified):',
'- line 65: const ADMIN_WALLET = lowercase 0x5f5a60eaef242c0d51a21c703f520347b96ed19a (operator consent-bypass).',
'- verifyConsent(biocid, creatorSig, ctx with serial+requester): if requester equals ADMIN_WALLET it returns granted=true source=admin-bypass. Operator-signed jobs already bypass BioNFT/consent. Reuse, do not weaken.',
'- spawnJob(job) lines 671-710: builds env spread of process.env plus BIOSAMPLE_ID, SAMPLE=job.biosampleId, BATCH_ID, R1_URI, R2_URI, JOB_ID, CREATOR_WALLET (default operator), INPUT_BUCKET, OUTPUT_BUCKET=genobank-parabricks-output, REF_BUCKET=genobank-references, SEQUENCING_TYPE, INPUT_TYPE (bam or fastq), IN_BAM_URI, REFERENCE=auto; then proc = spawn(JOB_RUNNER, [], { env, stdio ignore/pipe/pipe }); JOB_RUNNER = /usr/local/bin/clara-run-job.sh (symlink to clara-run-job-wgs.sh on the GPU VM).',
'- processJob(job) 712+: upsertJob verifying, verifyPayment, verifyConsent over consentBiocids (bam: [bamBiocid]; else [r1Biocid,r2Biocid]), spawnJob, upsertJob uploaded, registerDerivative (SKIPPED when job.parentIpId is a 0x + 64 hex Sequentia-native id), anchorJobOnChain (ClaraJobNFT.mintJob; needs CLARA_ANCHOR_PRIVATE_KEY + CLARA_JOB_NFT_ADDRESS else skipped).',
'- HTTP route /agent/job (POST) parses JSON body { jobId, biosampleId, inputType, bamUri, bamBiocid, creatorWallet, creatorSig, parentIpId, reference } and calls processJob.',
'- The methyl path MUST be ADDITIVE: existing bam/fastq jobs MUST be byte-for-byte untouched. Cleanest: in spawnJob, when inputType===methyl, spawn the biofs binary with args [methyl, exec, --sample, SAMPLE, --bams, METHYL_BAM_URIS, --ref, REF, --job-id, JOB_ID, --batch-id, BATCH_ID, --creator, CREATOR_WALLET, --out-bucket, OUTPUT_BUCKET, --ref-bucket, REF_BUCKET] with same env+stdio; everything else unchanged. In processJob, for inputType===methyl, skip the deepvariant registerDerivative and instead register/anchor a bedMethyl derivative (pipeline label ont-methylation-modkit) or at minimum upsert resultBiocid + anchor. The job carries methylBamUris (csv of gs:// URIs) and reference.',
'',
'RUNNER/OUTPUT CONVENTIONS (mirror clara-run-job-wgs.sh):',
'- OUTPUT_BUCKET default genobank-parabricks-output. OUT_GCS = gs://OUTPUT_BUCKET/jobs/BATCH_ID/SAMPLE/output. LOG_GCS = gs://OUTPUT_BUCKET/biowallet/WALLET_LC/logs/clara-jobs/JOB_ID. Write manifest.json to LOG_GCS, outputs to OUT_GCS. Containers via docker run --rm. The methyl exec subcommand MUST NOT download genomic bytes to local disk: gcsfuse-mount the input bucket read-only and stream (the 5 ONT BAMs total ~349 GiB); only the small bedMethyl outputs are streamed back to GCS.',
'',
'METHYL PIPELINE (what the methyl exec subcommand must do on the VM, via docker so no host tools needed). NOTE methyl is CPU-bound (minimap2 + modkit); it does NOT need a GPU, so it runs even though the GPU driver is currently broken. Inputs: John 5 ONT Dorado BAMs (unaligned, carry MM/ML base-mod tags):',
'  gs://t2t-genome-genobank/john-case/ont/dorado0.8.3_sup4.3.0_5mCG_5hmCG/05_29_24_R1041_UL_RttpProject_P1_1_dorado0.8.3_sup4.3.0_5mCG_5hmCG.bam',
'  (and P1_2, P1_3, plus 11_7_23_R1041_RttP_P1_0 and P1_1).',
'- Reference: GRCh38 consistent with John HiFi VCF, which used human_GRCh38_no_alt_analysis_set.fasta. Resolve via gcsfuse REF_BUCKET (genobank-references), fall back to Homo_sapiens_assembly38.fasta. Build a minimap2 .mmi once.',
'- Steps (preserve MM/ML throughout):',
'  1. For each ONT BAM: samtools fastq -T MM,ML BAM piped to minimap2 -y -ax map-ont -t nproc REF.mmi - piped to samtools sort -@ nproc -o aligned_i.bam. NEVER a bare samtools fastq (the -T MM,ML is mandatory or methylation tags are lost). minimap2 -y carries tags through.',
'  2. samtools merge -@ nproc merged.bam aligned_*.bam ; samtools index merged.bam.',
'  3. modkit pileup --cpg --combine-strands --ref REF merged.bam john.5mCG_5hmCG.bedMethyl. Keep BOTH 5mC and 5hmC (do NOT pass --ignore h). For allele-specific later add --partition-tag HP after phasing (v1 is genome-wide, unphased).',
'  4. bgzip + tabix the bedMethyl; compute a small summary (global percent mCG) for the manifest.',
'  5. gcloud storage cp the bedMethyl(.gz/.tbi) + manifest.json to OUT_GCS/LOG_GCS.',
'- Containers (not in the Parabricks image): minimap2 + samtools (use a real pullable image; samtools must be >= 1.16 for -T MM,ML) and modkit (ontresearch/modkit, pin a tag). State exact image refs and assume the human verifies they pull.',
'',
'DEPLOY FACTS: biofs-cli is NOT installed on the GPU VM (parabricks-gpu, internal 10.128.0.6, runs biofs-node:8787 + docker). DEPLOY.md must install the biofs binary there (npm i -g @genobank/biofs if the version resolves on npm, else copy the global dist tree) so spawn of biofs methyl exec works. genomic VMs: parabricks-gpu (a2-highgpu-1g, 12 vCPU, GPU driver broken but fine for CPU methyl) and parabricks-gpu-spot (a2-highgpu-2g, 24 vCPU, preemptible). Both in us-central1-f.',
'',
'CONSTRAINTS: laptop is codebase-only (never download genomic bytes locally); all genomic IO via gcsfuse on the VM. No proper names in any persisted clinical/genomic artifact (use serial/biocid/the proband); code identifiers and gs:// paths are fine. biofs-cli verbs auth via getCredentials() returning wallet_address + user_signature; the methyl submit subcommand runs under the operator signature so no BioNFT is required.',
].join('\n')

phase('Research')
const research = await parallel([
  () => agent(GROUND + '\n\nTASK research:verb-pattern. Read the real biofs-cli GROUP verbs to extract the EXACT authoring pattern for (a) a group with a DEFAULT subcommand that POSTs a job to biofs-node and (b) a sibling subcommand that runs a local pipeline via child_process. Read src/commands/job/recall.ts, src/commands/job/status.ts, src/commands/annotate/submit.ts, src/commands/fingerprint.ts, src/index.ts (find the const jobCmd = program.command(job) group registration and how subcommands are attached), and compiled /opt/homebrew/lib/node_modules/@genobank/biofs/dist/commands/job/recall.js + dist/index.js. Document: imports (getCredentials, CONFIG.API_BASE_URL, axios, ora, chalk, Logger), the exact axios.post shape to api_biofs_node/job, how options are typed, how a GROUP plus subcommands register in index.ts (program.command(group) then .command(sub) then option then action; whether commander supports a default subcommand via { isDefault: true } so "biofs methyl <serial>" routes to submit while "biofs methyl exec" routes to exec — verify and state how), and the precise hand-port recipe into the global dist (which files under dist/commands/methyl/, how to merge the group into dist/index.js, .d.ts needs). For the exec subcommand note how to shell out (child_process spawn/execFileSync), stream stdout so biofs-node captures it, and exit non-zero on failure. Return a precise spec plus boilerplate skeletons (not full impl).', {label:'research:verb-pattern', phase:'Research'}),
  () => agent(GROUND + '\n\nTASK research:node-integration. Read /Users/danieluribe/Downloads/biofs-node/src/index.js around spawnJob (671-710), processJob (712-820), verifyConsent (232-290), registerDerivative (306-350), anchorJobOnChain (370-440), and the /agent/job HTTP handler. Produce the EXACT additive patch plan for inputType===methyl: (1) the job fields to carry (methylBamUris csv, reference); (2) the spawnJob change to spawn the biofs binary with [methyl, exec, ...flags] ONLY when inputType===methyl (leave bam/fastq spawn(JOB_RUNNER) untouched); (3) the processJob change to verify consent over the methyl input (or rely on admin bypass), skip deepvariant registerDerivative, and register+anchor the bedMethyl derivative; (4) the /agent/job body fields to accept. Give the patch as minimal, clearly-bounded additive hunks with surrounding context so it applies to BOTH local (2009) and deployed (1899), plus a safe-deploy note (backup, node --check gate, systemctl restart biofs-node-v0.4, never a 0-byte deploy). Return the patch plan + exact hunks.', {label:'research:node-integration', phase:'Research'}),
])

phase('Draft')
const draft = await agent(GROUND + '\n\nRESEARCH verb-pattern:\n' + (research[0]||'(none)') + '\n\nRESEARCH node-integration:\n' + (research[1]||'(none)') + '\n\nTASK draft. Produce COMPLETE, deployable artifacts. Output each as a line FILE: path then a fenced code block. Deliver: (1) /Users/danieluribe/Downloads/biofs-cli/src/commands/methyl/submit.ts the CLIENT (default subcommand) for biofs methyl <serial> --bams csv [--ref name] [--json]: getCredentials, build job { jobId methyl-serial-ts, biosampleId, inputType methyl, methylBamUris, creatorWallet, creatorSig, reference, parentIpId optional }, axios.post to API_BASE_URL + /api_biofs_node/job, print job id and monitor hint. (2) /Users/danieluribe/Downloads/biofs-cli/src/commands/methyl/exec.ts the EXECUTOR subcommand for biofs methyl exec --sample --bams --ref --job-id --batch-id --creator --out-bucket --ref-bucket: full pipeline via child_process (gcsfuse mount RO, build/cache mmi, per-BAM samtools fastq -T MM,ML pipe minimap2 -y -ax map-ont pipe samtools sort, merge+index, modkit pileup --cpg --combine-strands keep 5mC+5hmC, bgzip+tabix, summary, gcloud storage cp to OUT_GCS + manifest.json to LOG_GCS); stream subprocess output to stdout; exit 0 on success else non-zero; docker run for tools with exact pullable image refs; idempotent. (3) the src/index.ts registration hunk: import the two subcommand actions, create const methylCmd = program.command(methyl).description(...), attach submit as the default subcommand (so biofs methyl <serial> works) and exec as a subcommand. (4) the biofs-node additive patch hunks for /opt/biofs-node-v0.4/src/index.js (also valid for the local 2009-line copy): spawnJob methyl branch that spawns the biofs binary with [methyl, exec, ...], processJob methyl handling, /agent/job body fields. (5) DEPLOY.md: hand-compile dist/commands/methyl/submit.js + exec.js into the global dist, merge the methyl group registration into global dist/index.js, install biofs-cli on the GPU VM, apply the biofs-node patch (backup, node --check, systemctl restart), and the exact biofs methyl <serial> --bams invocation with the 5 gs:// URIs. No placeholders.', {label:'draft:artifacts', phase:'Draft'})

phase('Review')
const LENSES = [
  { k:'genomics-correctness', p:'Verify the genomics: samtools fastq MUST carry -T MM,ML or methylation tags are lost; minimap2 must use -y and -ax map-ont; samtools >= 1.16; modkit pileup must keep BOTH 5mC and 5hmC (--cpg --combine-strands, NO --ignore h) and use --ref; the reference must be GRCh38 consistent with the HiFi VCF (human_GRCh38_no_alt_analysis_set.fasta); the mmi index built from that same ref; the docker image refs must actually exist and contain the right tools. Flag any tag-loss, wrong flag, wrong ref, or non-existent image, with the exact corrected command.' },
  { k:'prod-safety', p:'Verify the biofs-node patch is ADDITIVE and cannot break existing bam/fastq jobs: the spawn(JOB_RUNNER) path for bam/fastq is byte-for-byte unchanged; the methyl branch triggers only on inputType===methyl; the JS passes node --check; verifyConsent/admin-bypass intact; registerDerivative/anchor changes never throw for non-methyl jobs; the deployed(1899)-vs-local(2009) reconciliation is safe (backup + node --check + systemctl restart, never 0-byte). Flag any regression, syntax error, or unsafe deploy with the fix.' },
  { k:'protocol-and-cli-shape', p:'Verify Daniel directives: (i) methyl is a single command GROUP with subcommands submit (default) + exec, NOT a flat methyl-exec verb and NOT verb sprawl; commander must route biofs methyl <serial> to submit and biofs methyl exec to exec (verify the isDefault/default-command mechanism is correct and unambiguous, e.g. no biosample serial collides with the literal exec). (ii) NO orphan shell script: the executor is the biofs methyl exec subcommand spawned as the biofs binary with [methyl, exec, ...]. (iii) job is auditable (manifest.json + ClaraJobNFT anchor); the submit subcommand supports the operator-signature admin bypass so no BioNFT is required; no genomic bytes touch local/laptop disk (gcsfuse only, outputs streamed to GCS, no 349GiB download); biocid/serial used (no proper names) in persisted artifacts; the hand-port to the broken-npm global dist + biofs-cli install on the GPU VM are spelled out. Flag any deviation with the fix.' },
]
const reviews = await parallel(LENSES.map(l => () => agent(GROUND + '\n\nDRAFT ARTIFACTS:\n' + draft + '\n\nADVERSARIAL REVIEW LENS = ' + l.k + '.\n' + l.p + '\nBe specific: cite the file + exact line/command that is wrong and give the corrected version. If correct on your lens, say so and list what you verified.', {label:'review:'+l.k, phase:'Review'})))

phase('Finalize')
const final = await agent(GROUND + '\n\nDRAFT ARTIFACTS:\n' + draft + '\n\nADVERSARIAL REVIEWS:\n' + reviews.filter(Boolean).map((r,i)=>'--- review '+(i+1)+' ---\n'+r).join('\n\n') + '\n\nTASK finalize. Apply EVERY valid fix from the reviews and output the FINAL, complete, deployable artifacts. Same format (FILE: path then fenced block) for ALL of: src/commands/methyl/submit.ts, src/commands/methyl/exec.ts, the src/index.ts methyl-group registration hunk, the biofs-node additive patch hunks, and DEPLOY.md. Copy-paste deployable, no placeholders. End with a short RESIDUAL RISKS list (anything needing a live check on the GPU VM, e.g. exact docker image tags, gcsfuse mount flags, or commander default-subcommand behavior).', {label:'finalize', phase:'Finalize'})

return { final }
